module.exports = async function(helpers) {
  const { runCommand, ask, pause, dc, execSync, fs, path, isWindows, sleep, os } = helpers;
console.log('⚠️ Not applicable: Docker dev/test containers manage permissions automatically. If corrupted, rebuild the container.');
};
